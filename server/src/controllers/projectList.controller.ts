/* import express from 'express';

import ProjectList from "../models/ProjectList";


const getAll = async (req, res) => {
    try {
        const projectLists = await ProjectList.find();
    } catch (err) {
        res.status(500).json({ message: err.message });
    }
}; */